import WeatherInfo from "@/components/hero/WeatherInfo";
import React from "react";

const page = () => {
  return (
    <div className="container my-[140px] flex">
      <WeatherInfo />
    </div>
  );
};

export default page;
